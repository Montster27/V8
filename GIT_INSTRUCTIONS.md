# Git Branch Creation Instructions

Please execute the following git command in the terminal to create and switch to the UI-update branch:

```bash
cd /Users/montysharma/Documents/V8/MMV08
git checkout -b UI-update
```

This will:
1. Create a new branch named "UI-update"
2. Switch to this branch for our UI enhancement work

After executing this command, we can begin making UI improvements on this branch.
